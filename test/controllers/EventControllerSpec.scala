package controllers

import org.specs2.mutable.Specification

class EventControllerSpec extends Specification {
  "An EventController" should {
    "Read messages from Json, convert them to Events, then output Event JSON" in {
      failure
    }
  }
}
